import 'package:flutter/material.dart';

class Player {
  final String playerName;
  final Color? color;
  final IconData? iconData;

  const Player({required this.playerName, this.color, this.iconData});
}
